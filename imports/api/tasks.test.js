/* eslint-ОКР мокко */
 
импорт { Метеор } от 'Метеор/Метеор';
 
если (Метеор.isServer) {
 опишите('задачи', () => {
 описать("методы", () => {
 он(можно удалить владел задач', () => {
      });
    });
  });
}